============
Installation
============

At the command line::

    $ easy_install django_reform

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_reform
    $ pip install django_reform